# Compose-level settings (database credentials shared by db + api services).
# Copy to .env and change the password for any non-local deployment.
POSTGRES_USER=notesapp
POSTGRES_PASSWORD=notesapp
POSTGRES_DB=notesapp
