fastapi==0.141.1
# fastapi 0.141 pulls starlette 1.x — past the multipart-spool (GHSA-2c2j-9gv5-cj73)
# and Range-header (GHSA-7f5h-v6xp-fcq8) advisories in 0.41.
uvicorn[standard]==0.54.0
pydantic==2.13.5
pydantic-settings==2.13.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
bcrypt==4.2.1
pyotp==2.9.0
qrcode[pil]==8.0
# ≥0.0.27 closes the unbounded-part-header / preamble advisories used by the upload parser.
python-multipart==0.0.32
# Runtime: Web Push delivery (VAPID signing + the push-service POST).
pywebpush==2.0.3
httpx==0.28.1

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
ruff==0.8.4
